#include <stdio.h>
#include <stdlib.h>
int main(void)
{ int M,F,Y;
	printf("請輸入你的性別(M or F):" );
	scanf("%c,%c",&M,&F);
	printf("請輸入您的年齡:");
	scanf("%d",&Y);
	if(M&&(Y-(107-100))>=18&&F&&(Y-(107-100))>=16)

	
	printf("合法"); 
	else 
	
	printf("不合法");
	if(F&&(Y-(107-100))>=16)
	
	
	return 0;
}
